Trained artifact goes here as `anomaly_model_v1.joblib`, produced by
`../../ml_training/train_model.py`. Not present by default — `ml_detection.py` runs
purely on the self-calibrating `IsolationForest` until this file exists.
